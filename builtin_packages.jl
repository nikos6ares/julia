using Statistics: mean, std
mean(E_arr)   "Finds the mean and the standard deviation of an array"
std(M_arr)


trunc(Int, (3+8)/2) "returns back the nearest integer number"
range(0, stop=0.5, length=20) "returns 1:0.5:20, useful to avoid for i loop"

for (ib, b) in enumerate([3,8,3,82]) "enumerate returns back the position and the value of an array, kinda resembles "
    println("$ib and $b")
end

findmax(ht) "returns back the position and the maximum value of an array ht in this case"

size(beta) "Returns back the dimension of beta, useful to create zeros arrays"

